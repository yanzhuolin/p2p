# ========================================
# 前端客户端 - 环境变量配置示例
# ========================================
# 复制此文件为 .env.local 并根据实际情况修改
# 注意：Next.js 中，只有以 NEXT_PUBLIC_ 开头的变量才能在浏览器中访问

# ========================================
# 前端服务器配置
# ========================================

# 前端服务器端口
PORT=3000

# ========================================
# API 服务器配置（浏览器访问）
# ========================================

# API 服务器地址
# 开发环境：http://localhost:3001
# 生产环境：https://your-domain.com:3001
NEXT_PUBLIC_API_SERVER=http://localhost:3001

# API 服务器协议 (http | https)
NEXT_PUBLIC_API_PROTOCOL=http

# API 服务器主机名
NEXT_PUBLIC_API_HOST=localhost

# API 服务器端口
NEXT_PUBLIC_API_PORT=3001

# ========================================
# PeerJS 信令服务器配置（浏览器访问）
# ========================================

# PeerJS 信令服务器主机名
NEXT_PUBLIC_SIGNALING_SERVER=localhost

# PeerJS 信令服务器端口
NEXT_PUBLIC_SIGNALING_PORT=9000

# PeerJS 路径
NEXT_PUBLIC_PEER_PATH=/myapp

# PeerJS 调试级别 (0=关闭, 1=错误, 2=警告, 3=全部)
NEXT_PUBLIC_PEER_DEBUG_LEVEL=2

# ========================================
# 客户端配置
# ========================================

# 客户端心跳发送间隔（毫秒）
NEXT_PUBLIC_HEARTBEAT_INTERVAL=10000

# 在线用户列表刷新间隔（毫秒）
NEXT_PUBLIC_USER_LIST_REFRESH_INTERVAL=3000

# 连接超时时间（毫秒）
NEXT_PUBLIC_CONNECTION_TIMEOUT=10000

# ========================================
# SSL 证书配置 (HTTPS)
# ========================================

# SSL 证书路径（相对于 client 目录）
SSL_CERT_PATH=../certs/cert.pem
SSL_KEY_PATH=../certs/key.pem

# ========================================
# 环境配置
# ========================================

# 环境模式 (development | production)
NODE_ENV=development

