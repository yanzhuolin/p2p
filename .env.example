# ========================================
# 前端配置（浏览器连接使用）
# ========================================

# 前端服务器端口
PORT=3000

# API 服务器完整地址（前端连接地址，包括协议、主机名、端口）
NEXT_PUBLIC_API_ADDRESS=http://192.168.120.95:3001

# PeerJS 信令服务器完整地址（前端连接地址，包括协议、主机名、端口）
NEXT_PUBLIC_PEER_ADDRESS=http://192.168.120.95:9000

# ========================================
# 服务器配置（服务器监听使用）
# ========================================

# API 服务器端口
NEXT_PUBLIC_SERVER_API_PORT=3001

# PeerJS 信令服务器端口
NEXT_PUBLIC_SERVER_SIGNALING_PORT=9000

# ========================================
# 共同配置
# ========================================

# PeerJS 路径
NEXT_PUBLIC_SERVER_SIGNALING_PEER_PATH=/myapp

# ========================================
# SSL 证书配置 (HTTPS)
# ========================================

# SSL 证书路径（相对于项目根目录）
SSL_CERT_PATH=../certs/cert.pem
SSL_KEY_PATH=../certs/key.pem
